# Copia a .env junto a docker-compose.yml y ajusta valores.
# Uso: docker compose --env-file .env up -d

DB_PASSWORD=cambia_esto_seguro
JWT_SECRET=minimo_veinte_caracteres_seguros

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

OPENAI_API_KEY=
AI_PROVIDER=javascript

# CORS: origen del frontend que consume la API
FRONTEND_URL=http://localhost:5173

# Redis (compose inyecta redis://redis:6379 si omitís esto)
# REDIS_URL=redis://redis:6379
